{
  "name": "@agentverse/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Smart contracts for AgentVerse",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:x402": "hardhat run scripts/deploy-x402.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "dotenv": "^16.4.0"
  }
}
